services:
  mass-mcp:
    build: .
    # HTTP transport so we can test from the host
    command: ["mass-mcp-web"]
    ports:
      - "8086:8086"
    environment:
      MASS_URL: "${MASS_URL:-http://localhost:8095}"
      MASS_TOKEN: "${MASS_TOKEN}"
